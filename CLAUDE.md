# CLAUDE.md

@AGENTS.md

Claude-specific notes:
- Keep token usage lean: prefer focused inline edits over multi-agent workflows; the owner has flagged cost.
- Verify with `npm run typecheck && npm run lint && npm test && npm run build` before declaring work done.
- Reference files as clickable paths (e.g. `src/db/db.ts:42`).
